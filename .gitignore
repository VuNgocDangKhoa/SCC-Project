cat > .gitignore << 'EOF'
# 1. Bỏ qua các file dữ liệu và video khổng lồ (Nguyên nhân gây lỗi)
*.yuv
*.mp4
*.mkv
*.avi
*.mov
*.bin

# 2. Bỏ qua thư mục outputs NHƯNG cho phép push các file báo cáo an toàn
outputs/*
!outputs/results.csv
!outputs/visualizations/
dataset/

# 3. Bỏ qua các thư mục build và file thực thi (C/C++)
build/
bin/
*.o
*.out
*.exe
*.dll
*.so

# 4. Bỏ qua các file tạm của hệ điều hành
.DS_Store
Thumbs.db

# 5. Bỏ qua các thư mục cấu hình của IDE/Editor (VS Code, CLion,...)
.vscode/
.idea/
*.suo
*.user

# 6. Bỏ qua cache của Python và môi trường ảo
__pycache__/
*.pyc
.venv/
venv/
EOF